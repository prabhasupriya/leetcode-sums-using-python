 int a,b;
    cin >> a;
    cin >> b;
    vector<int> arr(b);
    for(int i=0;i<b ;i++){
        cin >> arr[i];
    }
    for(int i=0;i<b;i++){
        if(arr[i] == 0){
            bool left = (i==0 || arr[i-1] == 0);
            bool rigth = (i== b-1 || arr[i+1]== 0);
            if(left && rigth){
                arr[i] =1;
                a--;
                if(a==0){
                    cout <<"YES";
                    return 0;
                }
            
            }
        }
    }
    cout<<"NO";
    return 0;
