class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        maxval = float('-inf')
        currentsum =0
        for i in nums:
            currentsum += i
            if(currentsum > maxval):
                maxval = currentsum
            if(currentsum < 0):
                currentsum = 0
        return maxval
