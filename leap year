#include <bits/stdc++.h>
using namespace std;

int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int year;
    cin >> year;
    if((year %4 ==0 && year%100 !=0) ||  year%400 ==0 ){
        cout <<"Yes";
    }
    else{
        cout<< "No";
    }
    return 0;
}
