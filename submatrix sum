#include <bits/stdc++.h>
using namespace std;

int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int n;
    cin >>n;
    int i,j,k,l;
    cin >> i>> j>> k>> l;
    int sum=0;
    for(int r =0 ; r<n;r++){
        for(int c=0; c<n;c++){
        int x;
        cin >>x;
        if(r >= i && r <=k && c >=j && c <= l){
            sum +=x;
        }
        }
    }
    cout << sum;
    return 0;
}
