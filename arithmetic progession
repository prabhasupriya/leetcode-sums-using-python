#include <bits/stdc++.h>
using namespace std;

int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int a ,d,n;
    cin >> a>> d >>n ;
    int ap = a+((n-1)*d);
    cout << ap;
    return 0;
}
